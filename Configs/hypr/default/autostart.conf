# Foreground
execr-once = hyprpaper
execr-once = ~/Scripts/toggle_bar.sh
execr-once = firefox

# Background
execr-once = hypridle
execr-once = hyprshell run
execr-once = vesktop --start-minimized
# execr-once = steam -silent
